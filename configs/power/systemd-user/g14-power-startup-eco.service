[Unit]
Description=G14 power startup default (Eco)
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=/bin/bash -lc 'powerprofilesctl set power-saver || true; %h/.local/bin/g14-power-mode.sh apply --logout-on-pending no || true'

[Install]
WantedBy=default.target
